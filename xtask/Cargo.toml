[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license = "AGPL-3.0-or-later"
publish = false
description = "workspace task runner (gen-proto, check-proto, release helpers)"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
tonic-build = { workspace = true }
walkdir = { workspace = true }
# The ISC registry leaf crate (zero deps) — single source of TOTAL / COVERED,
# read live so xtask no longer carries a hand-maintained count mirror.
daemonseed-isc = { path = "../crates/daemonseed-isc" }
yaml-rust2 = "0.11.0"
